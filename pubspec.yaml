# pubspec.yaml
name: smart_cart
description: Shopping List Manager with Price Tracking and Store Comparison
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^13.0.0
  fl_chart: ^0.68.0
  iconsax: ^0.0.8
  google_fonts: ^6.2.1
  gap: ^3.0.1
  flutter_slidable: ^3.1.0
  lottie: ^3.1.0
  intl: ^0.19.0
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
